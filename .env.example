# Optional: enables AI-personalized coaching via Google Gemini.
# The app works fully without this — it falls back to a built-in rules engine.
# Get a FREE key (no credit card) at https://aistudio.google.com/app/apikey
# In production, proxy this through a serverless function rather than exposing it client-side.
VITE_GEMINI_API_KEY=
